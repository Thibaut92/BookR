<!DOCTYPE html>
<html lang="fr" data-theme="light">
  <head>
    <title>BookR</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <div class="container">
        <%= link_to root_path, class: "logo" do %>
          BookR
        <% end %>

        <ul>
          <% if user_signed_in? %>
            <li><%= link_to "Mon compte", edit_user_registration_path, class: "nav-button" %></li>
            <li><%= link_to "Déconnexion", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-button" %></li>
          <% else %>
            <li><%= link_to "Se connecter", new_user_session_path, class: "nav-button" %></li>
            <li><%= link_to "S'inscrire", new_user_registration_path, class: "nav-button signup" %></li>
          <% end %>
        </ul>
      </div>
    </nav>

    <main>
      <%= render "shared/flashes" %>
      <%= yield %>
    </main>

    <%= render 'shared/footer' %>
  </body>
</html>
